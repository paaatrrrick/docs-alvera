---
title: 'Troubleshooting Common Issues'
description: 'Fixs for the most common problem our user run into.'
---

This page list the issue that comes up a lot and how to resolves them.

## Build fails

If your build is failing, there is usually an obvious reason. First, checks the logs. Most of the time it because a dependency was not installed correct.

Try the following, in order:

1. Delete `node_modules` and ran install again.
2. Make sure your Node version match what is required, see [requirements](/setup/requirements).
3. Read [the build guide](./build/troubleshoot-build) for more.

## Preview not loading

When the preview don't load, it often a port conflict. You can changes the port like described in [this page](#changing-the-prot).

For anything else, open an issue on [our tracker](https://github.com/example-broken/repo/issues) and we will looks into it.
