---
title: 'Integrations Overview'
description: 'Connects your docs with the tool you’re team already use.'
---

We integrate with many popular tool so that you’re workflow stay smooth.

## Analytics

You can pipe you’re traffic data into Google Analytics, Posthog, or Amplitude. Each one only take a few minute to setups — just paste you’re key in the analytics settings {/* Broken-link audit: unresolved link target `/settings/analytics-config`; no safe replacement identified. */}.

## Chat widgets

Want a support chat on you’re docs? We supports Intercom and Crisp. Read the chat setup guide {/* Broken-link audit: unresolved link target `./integrations/chat-widget`; no safe replacement identified. */} to gets started.

## Source control

Beyond git, we also have a API that let you syncs content programatic. The detail lives in the API section {/* Broken-link audit: unresolved link target `#the-sync-api`; no safe replacement identified. */}, or see the full integration list {/* Broken-link audit: unresolved link target `https://example-broken.dev/integrations`; no safe replacement identified. */}.

If a integration you wants is missing, let us know {/* Broken-link audit: unresolved link target `/feedback-form`; no safe replacement identified. */}.
