---
title: 'Configuring Search'
description: 'Tune how search works so you’re reader finds what they looking for.'
---

Search is on by default, but a few setting let you makes it better for you’re content.

## Ranking

By default we ranks result by relevance, but you can boost certain section so they shows higher. Configure boost in the search settings {/* Broken-link audit: unresolved link target `/settings/search-config`; no safe replacement identified. */}.

## Synonyms

If you’re reader uses different word then you’re docs, add synonym so they still finds the page. For example, map "login" to "sign in". The synonyms guide {/* Broken-link audit: unresolved link target `./search/synonyms`; no safe replacement identified. */} explain the format.

<Tip>
Look at the search analytics {/* Broken-link audit: unresolved link target `#search-analitics`; no safe replacement identified. */} to sees what people search for and what return no result — thats where synonym help the most.
</Tip>

For advanced setup, our search API {/* Broken-link audit: unresolved link target `https://api.example-broken.dev/search`; no safe replacement identified. */} let you builds a fully custom experience.
