---
title: 'Deployment Guide'
description: 'Ship you’re docs to production in a couple step.'
---

Once you’re happy with how things look localy, its time to deploys.

## Connecting your repo

First you connect you’re git repository. We support GitHub and GitLab, the setup are slightly different for each — see the git setup page {/* Broken-link audit: unresolved link target `/integrations/git-setup`; no safe replacement identified. */}.

After it connected, every push to you’re main branch will triggers a new deploy automatic.

## Custom domains

You can points a custom domain at you’re site. Add the domain in settings {/* Broken-link audit: unresolved link target `/settings/domains-broken`; no safe replacement identified. */} and then update you’re DNS record like shown in this walkthrough {/* Broken-link audit: unresolved link target `./domains/dns-setup`; no safe replacement identified. */}.

<Tip>
Deploys usually take less then a minute, but a big site can takes longer. You can watch the progress in the deploy log {/* Broken-link audit: unresolved link target `#deploi-logs`; no safe replacement identified. */}.
</Tip>

If a deploy fail, read the deploy troubleshooting {/* Broken-link audit: unresolved link target `https://help.example-broken.dev/deploys`; no safe replacement identified. */} page.
