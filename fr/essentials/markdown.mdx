---
title: 'Référence de la syntaxe Markdown pour Mintlify'
description: 'Référence pour les titres, la mise en forme du texte, les listes, les liens, les citations et les autres mises en forme Markdown standard prises en charge dans les pages de documentation Mintlify.'
icon: 'text-size'
---

## Titres

Idéal pour les en-têtes de section.

```md
## Titles
```

### Sous-titres

Idéal pour les en-têtes de sous-section.

```md
### Subtitles
```

<Tip>

Chaque **titre** et **sous-titre** crée une ancre et apparaît également dans la table des matières à droite.

</Tip>

## Mise en forme du texte

Mintlify prend en charge la plupart des mises en forme Markdown. Ajoutez `**`, `_` ou `~` autour du texte pour le mettre en forme.

| Style         | Comment l'écrire  | Résultat        |
| ------------- | ----------------- | --------------- |
| Gras          | `**bold**`        | **gras**        |
| Italique      | `_italic_`        | _italique_      |
| Barré         | `~strikethrough~` | ~barré~         |

Vous pouvez les combiner. Par exemple, écrivez `**_bold and italic_**` pour obtenir du texte **_gras et italique_**.

Vous devez utiliser du HTML pour écrire du texte en exposant et en indice. C'est-à-dire, ajoutez `<sup>` ou `<sub>` autour de votre texte.

| Taille de texte | Comment l'écrire         | Résultat               |
| --------------- | ------------------------ | ---------------------- |
| Exposant        | `<sup>superscript</sup>` | <sup>exposant</sup>    |
| Indice          | `<sub>subscript</sub>`   | <sub>indice</sub>      |

## Liens vers des pages

Vous pouvez ajouter un lien en entourant du texte avec `[]()`. Vous écririez `[link to google](https://google.com)` pour [créer un lien vers google](https://google.com).

Les liens vers les pages de vos docs doivent être relatifs à la racine. En gros, vous devez inclure le chemin complet du dossier. Par exemple, `[link to text](/writing-content/text)` pointe vers la page « Text » dans notre section des composants.

Les liens relatifs comme `[link to text](../text)` s'ouvriront plus lentement car nous ne pouvons pas les optimiser aussi facilement.

## Citations

### Sur une ligne

Pour créer une citation, ajoutez un `>` devant un paragraphe.

> Dorothée la suivit à travers de nombreuses belles pièces de son château.

```md
> Dorothy followed her through many of the beautiful rooms in her castle.
```

### Sur plusieurs lignes

> Dorothée la suivit à travers de nombreuses belles pièces de son château.
>
> La Sorcière lui ordonna de nettoyer les marmites et les bouilloires, de balayer le sol et d'alimenter le feu en bois.

```md
> Dorothy followed her through many of the beautiful rooms in her castle.
>
> The Witch bade her clean the pots and kettles and sweep the floor and keep the fire fed with wood.
```

### LaTeX

Mintlify prend en charge [LaTeX](https://www.latex-project.org) via le composant Latex.

<Latex>8 x (vk x H1 - H2) = (0,1)</Latex>

```md
<Latex>8 x (vk x H1 - H2) = (0,1)</Latex>
```
