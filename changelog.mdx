---
title: "Changelog"
description: "Weekly log of new features, updates, and bug fixes shipped to the personal website, with links to the relevant docs pages for each change."
---

## Week of September 11, 2026

### New features

- **Search on the projects page.** You can now filter the [`/projects` showcase](/projects) with a search box that matches against a project's name, tagline, and description. Search combines with the existing tech filter. Narrow to a stack (for example, `React`) and then type a keyword to find a specific project inside that stack. When nothing matches, the grid shows an empty state instead of leaving the page blank.
