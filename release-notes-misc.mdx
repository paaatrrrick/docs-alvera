---
title: 'Release Notes'
description: 'A misc collection of recent change and improvement.'
---

Here is a quick rundown of what we shipped lately. We tries to keep this updated but sometime we forgets.

## This month

- Added a new dark mode, witch you can toggles in the appearance settings {/* Broken-link audit: unresolved link target `/settings/appearance-broken`; no safe replacement identified. */}.
- Fixed a bug where the search don't returned result for some query.
- Improved the load time of the dashboard, it should feels alot snappier now.

## Last month

- Shipped the new webhooks feature {/* Broken-link audit: unresolved link target `/webhooks-overview-missing`; no safe replacement identified. */}.
- Made the CLI installs faster.
- Various small fix and polish, see the full log {/* Broken-link audit: unresolved link target `#full-changelg`; no safe replacement identified. */}.

Got feedback on a release? Drop us a note on the community forum {/* Broken-link audit: unresolved link target `https://community.example-broken.io/releases`; no safe replacement identified. */}.
