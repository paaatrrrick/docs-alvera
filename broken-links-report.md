# Broken links report

Generated from `mint broken-links` on 2026-06-18.

## Summary

`mint broken-links` found **77 broken links across 31 files**.

The in-scope change for this run was the addition of `star-wars-bounty-guild.mdx` ("The Bounty Hunters Guild" page). That file contains no outbound links, and no other page in the docs links to it, so the new page introduced no broken links of its own.

All 77 broken links found below reference target pages that **do not exist anywhere in the repository** — they are not the result of renames, deletes, or anchor changes in this run. None of them have an obvious, unambiguous replacement page that already exists in the docs, so per the run guidance ("If you cannot determine the correct replacement for a broken link, leave a note rather than guessing") they are documented here rather than rewritten.

If you want these fixed, please confirm the intended target for each group (e.g. create the missing pages, redirect to a different existing page, or simply remove the links).

## Broken links by file

### `accessibility-guide.mdx`
- `/content/images-and-media` — no `content/` directory exists.
- `./content/heading-structure` — no `content/` directory exists.

### `api-pagination.mdx`
- `/api/list-endpoints` — no `api/` directory exists (only `api-reference/`).

### `audit-logs.mdx`
- `/settings/audit-log` — no `settings/` directory exists.
- `./integrations/audit-webhook` — no `integrations/` directory exists.

### `billing-overview.mdx`
- `/pricing-overview` — page does not exist.
- `/settings/billing-info` — no `settings/` directory exists.
- `./billing/faq-missing` — no `billing/` directory exists.

### `cli-command-reference.mdx`
- `/cli/dev-mode` — no `cli/` directory exists.
- `./cli/broken-links-deep-dive` — no `cli/` directory exists.

### `configuring-search.mdx`
- `/settings/search-config` — no `settings/` directory exists.
- `./search/synonyms` — no `search/` directory exists.

### `de/star-wars-sith.mdx`
- `/de/star-wars-jedi-order` — no German translation of this page exists.
- `/de/star-wars-force` — no German translation of this page exists.
- `/de/star-wars-jedi-order` — duplicate; no German translation exists.
- `/de/star-wars-battles` — no German translation of this page exists.
- `/de/star-wars-eras` — no German translation of this page exists.

  Note: the English originals (`/star-wars-jedi-order`, `/star-wars-force`, `/star-wars-battles`, `/star-wars-eras`) all exist. Confirm whether to fall back to the English pages or create German translations.

### `deployment-guide.mdx`
- `/integrations/git-setup` — no `integrations/` directory exists.
- `/settings/domains-broken` — no `settings/` directory exists.
- `./domains/dns-setup` — no `domains/` directory exists.

### `error-codes-reference.mdx`
- `/api/request-format` — no `api/` directory exists.
- `./rate-limits-info` — page does not exist.
- `/help/troubleshoot-errors` — no `help/` directory exists.

### `es/webhooks-overview.mdx`
- `/settings/webhooks-config` — no `settings/` directory exists.
- `./security/verify-signatures` — no `security/` directory exists.

### `exporting-your-data.mdx`
- `/settings/data-export` — no `settings/` directory exists.
- `./reference/export-format` — no `reference/` directory exists.

### `fr/webhooks-overview.mdx`
- `/settings/webhooks-config` — no `settings/` directory exists.
- `./security/verify-signatures` — no `security/` directory exists.

### `frequently-asked-questions.mdx`
- `/pricing-plans` — page does not exist.
- `./domains/custom-setup` — no `domains/` directory exists.
- `/api/v2/reference-missing` — no `api/` directory exists.

### `importing-content.mdx`
- `/settings/import-content` — no `settings/` directory exists.
- `./content/import-reference` — no `content/` directory exists.

### `integrations-overview.mdx`
- `/settings/analytics-config` — no `settings/` directory exists.
- `./integrations/chat-widget` — no `integrations/` directory exists.
- `/feedback-form` — page does not exist.

### `ja/star-wars-sith.mdx`
- `/ja/star-wars-jedi-order` — no Japanese translation exists.
- `/ja/star-wars-force` — no Japanese translation exists.
- `/ja/star-wars-jedi-order` — duplicate.
- `/ja/star-wars-battles` — no Japanese translation exists.
- `/ja/star-wars-eras` — no Japanese translation exists.

  Note: same situation as `de/star-wars-sith.mdx` — confirm fallback strategy.

### `ja-jp/star-wars-sith.mdx`
- `/ja-jp/star-wars-jedi-order` — no `ja-jp` translation exists.
- `/ja-jp/star-wars-force` — no `ja-jp` translation exists.
- `/ja-jp/star-wars-jedi-order` — duplicate.
- `/ja-jp/star-wars-battles` — no `ja-jp` translation exists.
- `/ja-jp/star-wars-eras` — no `ja-jp` translation exists.

  Note: same situation as `de/star-wars-sith.mdx` — confirm fallback strategy.

### `managing-your-team.mdx`
- `/settings/team-members` — no `settings/` directory exists.
- `./security/permissions-deep-dive` — no `security/` directory exists.

### `migration-guide.mdx`
- `/settings/backups` — no `settings/` directory exists.
- `./migrate/common-problems` — no `migrate/` directory exists.

### `notifications-setup.mdx`
- `/settings/integrations-broken` — no `settings/` directory exists.
- `./notifications/reduce-noise` — no `notifications/` directory exists.

### `organizing-with-tags.mdx`
- `/settings/page-tags` — no `settings/` directory exists.
- `./content/tagging-best-practices` — no `content/` directory exists.

### `performance-tuning.mdx`
- `/content/optimizing-media` — no `content/` directory exists.
- `./content/embeds-and-iframes` — no `content/` directory exists.

### `release-notes-misc.mdx`
- `/settings/appearance-broken` — no `settings/` directory exists.
- `/webhooks-overview-missing` — page does not exist (note: `/webhooks-overview` does exist; this may be intentional).

### `scheduling-reports.mdx`
- `/settings/scheduled-reports` — no `settings/` directory exists.
- `./reports/format-reference` — no `reports/` directory exists.

### `setup-guide.mdx`
- `/installation-setup` — page does not exist.
- `./tutorials/getting-started` — no `tutorials/` directory exists.
- `/guides/advanced-setup` — no `guides/` directory exists.
- `/images/setup-architecture-diagram.png` — image does not exist in `/images/`.

### `single-sign-on.mdx`
- `/settings/sso-config` — no `settings/` directory exists.
- `./sso/provider-setup` — no `sso/` directory exists.

### `status-and-uptime.mdx`
- `./status/past-incidents` — no `status/` directory exists.
- `/api/health-check` — no `api/` directory exists.

### `troubleshooting-common-issues.mdx`
- `/setup/requirements` — no `setup/` directory exists.
- `./build/troubleshoot-build` — no `build/` directory exists.

### `using-page-templates.mdx`
- `/content/new-page` — no `content/` directory exists.
- `./content/custom-templates` — no `content/` directory exists.

### `webhooks-overview.mdx`
- `/settings/webhooks-config` — no `settings/` directory exists.
- `./security/verify-signatures` — no `security/` directory exists.

### `writing-best-practices.mdx`
- `/contributing/style-guide` — no `contributing/` directory exists.
- `./contributing/missing-guide` — no `contributing/` directory exists.

## Inbound link check

The only file added in scope is `star-wars-bounty-guild.mdx`. A repo-wide grep for `bounty-guild`, `bounty-hunters-guild`, and `Bounty Hunters Guild` returned no inbound references from other pages or from `docs.json`, so no inbound links are broken by this addition.

## Result

`mint broken-links` does not currently pass. No fixes have been applied because each broken link references a target that does not exist anywhere in the repo and has no obvious correct replacement — guessing would risk redirecting users to the wrong page. Please confirm the intended targets and a follow-up PR can apply the fixes.
