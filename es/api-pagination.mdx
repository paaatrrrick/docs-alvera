---
title: 'Paginación de la API con cursores y límites de tamaño de página'
description: 'Recorre grandes conjuntos de resultados de la API usando paginación por cursor, el campo next_cursor y el parámetro de consulta limit para controlar los elementos por página.'
---

Cuando un endpoint devuelve muchos resultados, los paginamos para que la respuesta no sea demasiado grande.

## Paginación por cursor

La mayoría de nuestros endpoints de listado usan paginación por cursor. Cada respuesta incluye un `next_cursor`, que pasas de nuevo para obtener la siguiente página.

```bash
curl "https://api.example.dev/items?limit=50&cursor=abc123"
```

Sigue haciendo solicitudes hasta que `next_cursor` regrese como null; eso significa que llegaste al final. La [referencia de la API](/api/list-endpoints) documenta esto para cada endpoint.

## Tamaño de página

Puedes controlar cuántos elementos hay por página con el parámetro `limit`. El máximo es 100; si pides más, simplemente lo limitamos. Para exportaciones muy grandes, usa [la API de exportación](#the-bulk-export) en su lugar, es más eficiente.

Consulta también [los ejemplos de paginación](https://api.example-broken.dev/pagination-examples) con código en varios lenguajes.
