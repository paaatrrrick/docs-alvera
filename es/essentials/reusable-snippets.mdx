---
title: "Crear snippets de contenido reutilizables"
description: "Define snippets MDX reutilizables, impórtalos en distintas páginas y pásales props o variables para mantener el contenido de la documentación sincronizado sin duplicarlo."
icon: "recycle"
---

import SnippetIntro from '/snippets/snippet-intro.mdx';

<SnippetIntro />

## Crear un snippet personalizado

**Requisitos previos**: Crea tu archivo de snippet en el directorio `snippets`.

<Note>
  Cualquier página en el directorio `snippets` será tratada como un snippet y no
  se renderizará como página independiente. Si quieres crear una página
  independiente a partir del snippet, importa el snippet en otro archivo y
  llámalo como un componente.
</Note>

### Exportación por defecto

1. Añade al archivo de snippet el contenido que quieras reutilizar en múltiples
   ubicaciones. Opcionalmente, puedes añadir variables que se rellenarán mediante props
   al importar el snippet.

```mdx snippets/my-snippet.mdx
Hello world! This is my content I want to reuse across pages. My keyword of the
day is {word}.
```

<Warning>
  El contenido que quieras reutilizar debe estar dentro del directorio `snippets`
  para que la importación funcione.
</Warning>

2. Importa el snippet en tu archivo de destino.

```mdx destination-file.mdx
---
title: My title
description: My Description
---

import MySnippet from '/snippets/path/to/my-snippet.mdx';

## Header

Lorem ipsum dolor sit amet.

<MySnippet word="bananas" />
```

### Variables reutilizables

1. Exporta una variable desde tu archivo de snippet:

```mdx snippets/path/to/custom-variables.mdx
export const myName = 'my name';

export const myObject = { fruit: 'strawberries' };
```

2. Importa el snippet desde tu archivo de destino y usa la variable:

```mdx destination-file.mdx
---
title: My title
description: My Description
---

import { myName, myObject } from '/snippets/path/to/custom-variables.mdx';

Hello, my name is {myName} and I like {myObject.fruit}.
```

### Componentes reutilizables

1. Dentro de tu archivo de snippet, crea un componente que reciba props exportando
   tu componente como una función flecha.

```mdx snippets/custom-component.mdx
export const MyComponent = ({ title }) => (
  <div>
    <h1>{title}</h1>
    <p>... snippet content ...</p>
  </div>
);
```

<Warning>
  MDX no compila dentro del cuerpo de una función flecha. Usa sintaxis HTML
  siempre que puedas o utiliza una exportación por defecto si necesitas usar MDX.
</Warning>

2. Importa el snippet en tu archivo de destino y pásale los props

```mdx destination-file.mdx
---
title: My title
description: My Description
---

import { MyComponent } from '/snippets/custom-component.mdx';

Lorem ipsum dolor sit amet.

<MyComponent title={'Custom title'} />
```
