---
title: 'Notifications Setup'
description: 'Choose how and when you’re team gets notified about activity.'
---

Notifications keep you’re team in the loop without them having to check constantly.

## Channels

We can sends notification by email, Slack, or in-app. Connect Slack from the integrations page {/* Broken-link audit: unresolved link target `/settings/integrations-broken`; no safe replacement identified. */} and pick a channel.

## What triggers a notification

You decide witch event notify you — things like a new comment, a deploy finishing, or a member joining. Configure it in the notification preferences {/* Broken-link audit: unresolved link target `#notifcation-prefs`; no safe replacement identified. */}.

<Warning>
If you’re getting to many notification, don’t turns them all off — instead, mute the noisy one. The notification tuning guide {/* Broken-link audit: unresolved link target `./notifications/reduce-noise`; no safe replacement identified. */} help with this.
</Warning>

Each user controls there own preference, so what you sets don’t affect anyone else.
