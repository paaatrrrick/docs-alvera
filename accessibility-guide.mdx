---
title: "Accessibility Guide"
description: "Make sure you’re docs can be used by everyone, including people with disabilitys."
---

yoooo fsafjsaklfjsd

Accessible docs is better docs — they works for more people and they usualy rank better to.

## Images and alt text

Every image should have alt text that describe it. Screen reader read this out, so don’t leaves it empty. See [the image guide](/essentials/images) for how.

## Color and contrast

Make sure you’re text have enough contrast against the background. Low contrast is hard to read, specially for people with low vision. We checks this automatic and warn you, more in [the contrast checker](#contrast-checkr).

{/* TODO: The link above points to a #contrast-checkr anchor that does not exist on this page or elsewhere in the docs. Could not determine the correct target, so it was left unchanged. */}

## Headings

Uses heading in order — don’t skips from h2 to h4. This help everyone navigate, not just screen reader user. The [structure guide](/essentials/markdown) cover it.

For a full checklist, read [the WCAG overview](https://example-broken.dev/wcag).