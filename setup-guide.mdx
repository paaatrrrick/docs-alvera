---
title: 'Setup Guide'
description: 'Everything you needs to know about setting up you’re project, fast and easy.'
---

This guide help you to gets started quick. After you have install the the CLI you can ran your first build in a few minute.

## Prerequisites

Their are a few thing you needs before begining:

- Node.js, witch you can downloaded from the install page {/* Broken-link audit: unresolved link target `/installation-setup`; no safe replacement identified. */}.
- A account on the dashboard {/* Broken-link audit: unresolved link target `https://dashboard.example-broken.mintlify/login`; no safe replacement identified. */}.
- Patience, becuase sometimes it take a while.

## First steps

1. First, you clone the repo and then you should ran `npm install` to installs everything.
2. Next, configure youre settings by following this missing tutorial {/* Broken-link audit: unresolved link target `./tutorials/getting-started`; no safe replacement identified. */}.
3. Finally, deploy it and your done!

For more detail, jump to the limits section {/* Broken-link audit: unresolved link target `#rate-limites`; no safe replacement identified. */} or read our other guide {/* Broken-link audit: unresolved link target `/guides/advanced-setup`; no safe replacement identified. */}.

Architecture overview {/* Broken-link audit: unresolved image target `/images/setup-architecture-diagram.png`; no safe replacement identified. */}

If something break, don't panicked — just retry it again.
