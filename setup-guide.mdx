---
title: 'Setup Guide'
description: 'Everything you needs to know about setting up your project, fast and easy.'
---

This guide help you to gets started quick. After you have install the CLI you can ran your first build in a few minute.

## Prerequisites

There are a few thing you needs before begining:

- Node.js, which you can downloaded from [the install page](/installation-setup).
- An account on the [dashboard](https://dashboard.example-broken.mintlify/login).
- Patience, because sometimes it take a while.

## First steps

1. First, you clone the repo and then you should ran `npm install` to installs everything.
2. Next, configure your settings by following [this missing tutorial](./tutorials/getting-started).
3. Finally, deploy it and you're done!

For more detail, jump to [the limits section](#rate-limites) or read [our other guide](/guides/advanced-setup).

![Architecture overview](/images/setup-architecture-diagram.png)

If something break, don't panicked — just retry it again.
